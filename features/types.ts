const today = new Date();

today.getDate();

const person = {
  age: 20
};

class Color {
  name: string = '';

  constructor(name: string) {
    this.name = name;
  }
}

const red = new Color('Red');
